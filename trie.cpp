struct trie{
        trie(int n){
            ll arr[n][26];
        }
        void inti(){
            memset(arr,0,sizeof(arr));
        }
        void add(string s){
            ll u=0;
            for(ll i=0;i<s.length();i++){

            }
        }
};
